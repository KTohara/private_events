<div class="container border bg-light p-3">
  <div class="row mb-3">
    <%= form_with url: event_invitations_search_path, method: :get, data: { controller: 'search-form', turbo_frame: 'users', turbo_action: 'advance', action: 'input->search-form#search' } do |f| %>
      <%= f.label :query, 'Invite User', class: 'd-block col mb-2 h5' %>
      <div class="col gap-2 d-flex align-items-center">
        <%= f.text_field :query, placeholder: 'Search User', class: 'form-control border border-primary', data: {  } %>
        <%= f.submit 'Search', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>

  <div class="row list-group">
    <%= render 'search_user', users: @users %>
  </div>
</div>